{% extends '::base.html.twig' %}
{% block body %}

<h1>Ingresos</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Tipo</th>
            <td>{{ entity.Tipo }}</td>
        </tr>
        <tr>
            <th>Fecha</th>
            <td>{{ entity.Fecha|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Descripcion</th>
            <td>{{ entity.Descripcion }}</td>
        </tr>
        <tr>
            <th>Monto</th>
            <td>{{ entity.Monto }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('ingresos') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ingresos_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('ingresos_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>

{% endblock %}