{% extends '::base.html.twig' %}
{% block body %}

{% if is_granted('ROLE_ADMIN') %}
<h1>Nuevo Estado Financiero</h1>

<form action="{{ path('estadofinanciero_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('estadofinanciero') }}">
            Back to the list
        </a>
    </li>
</ul>

{% endif %}
{% endblock %}